#################################### Section 1 ############################################
# print text, number, multiply number, power of number
# concatenate number with string. Is it possible integer concatenation
# how to use "f"? Print some text and number using "f".
# Multiply 2 numbers and print text and number with the help of "f".
# Repeat list 1 value 10 times and print with some text and result using "f".
# Repeat list 2 values 10 times and print with some text and result using "f".
# Repeat tuple 2 values 10 times and print with some text and result using "f". Check is it possible print 1 value 10 times in tuples.
# Repeat strings 10 times using "f".
# Reverse a string.
# Check a string is exist or not.
# Remove whitespace from string.
# Convert string to upper or lower.
# find index, startswith, endswith, count, replace.
# split string, join string.
# slicing a string using "[:Index], [:-Index], [Index:], [Start Index:End Index], [::-1], [:]".
# Check type of a variable.

#################################### Section 2 ############################################
# Ask customer name and age
# Check customer age parameter is digit or not.
# Check age type (str or int).
# Validate age. Age should be greater than 18.
# If everything is correct then print the message.
# Output message should be a separate function.
# Remove isdigit then Add error handling like try, except and check

################################ Section 3 (list) ###########################################
# Task 1: Enter no of days comma separated eg(1, 2, 3).
# Task 1: Split numbers and calculate hours.

################################ Section 4 (Dictionary) ######################################
# Get data from dictionary using get.
# Delete data from dictionary.
# Difference del, pop and popitem.
# Merge multiple dictionaries.

################################ Section 5 (Math, OS) ######################################
# Test few functions in library.

################################ Section 6 Tuples ######################################
# Define one element in Tuple.
# Convert tuple from list.
# Merge multiple tuples.
# Count a letter in tuple.
# Can we get index zero tuple?
# How to access tuple? 
# Destructing with tuple.
# Enumerate
# Merge tuples in to a list.

################################ Section 7 Set ######################################
# Check type of blank set "{} and set()".
# add multiple values in "set()".
# Add single values in set and add multiple values in set.
# Update set with multiple values.
# Access set using for loop.
# Check the element in set.
# Remove and Discard element from set. Then check the difference.
# Clear the set.
# Union the data with "union" and "|" built in feature.
# intersection, symmetric difference, difference in set.
# Check two set is isdisjoint, subset and superset.
# Merge tuple and set in to a list. 



